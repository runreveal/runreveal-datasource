import { css } from '@emotion/css';

export const styles = {
    QueryEditor: {
        queryType: css`
          justify-content: space-between;
          flex-direction: column;
          align-items: flex-end;
          span {
            display: flex;
          }
        `,
      },
    }